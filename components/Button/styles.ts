import styled from "styled-components";

export const Button = styled.button`
    border: none;
    background: #8b21ff;
    margin-top: 1rem;
    border-radius: 4px;
    padding: 0.3rem;
    width: auto;
    padding: 0.4rem 3rem;
    font-size: 1rem;
    color: #fff;
    font-weight: bold;

    &:hover{
        filter: brightness(85%);
    }
`;
